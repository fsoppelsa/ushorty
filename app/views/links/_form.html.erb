<%= form_for(@link, :remote => true) do |f| %>
  <% if false %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% end %>

  <div id="original">
    <%= f.label t(:original_link) %> &mdash; 
    <%= f.text_field :original, :size => 40 %>
  </div>
  <div id="button">
    <%= button_to t(:get_hashed), :remote => true, :disabled => true %>
  </div>
  <div id="hashed">
  </div>
<% end %>
